package com.example.dictionary.models

import org.junit.Test

class TestTranslationModel {

    /*@Test
    fun checkData() {
        *//*val repositoryCache: CacheRepository = mock(CacheRepository::class.java)
        /*val dataProvider: DataProvider = DataProvider(repositoryCache)*//*

        val langList: MutableList<InfoLanguage> = ArrayList(20)
        val langMap: MutableMap<String, InfoLanguage> = HashMap(20)
        val tranlsList: MutableList<InfoTranslation> = LimitedDataBaseArrayList(repositoryCache)

        tranlsList

        `when`

        assertEquals(presenter.getListLangs(), langList)*/
    }*/
}
